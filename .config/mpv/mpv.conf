hwdec=auto              # Enable hardware decoding if available  
keep-open=yes           # Keep the window open after playback ends  
loop-file=inf           # Loop the current file infinitely  

cursor-autohide=3000    # Hide mouse cursor after 3 seconds of inactivity  
osd-duration=3000       # On-screen messages (like pause/play) show for 3 seconds  
